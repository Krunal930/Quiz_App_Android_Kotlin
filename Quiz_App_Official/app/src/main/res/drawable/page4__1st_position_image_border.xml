<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outer layer (border) -->
    <item>
        <shape
            android:shape="oval">
            <solid android:color="@color/gold"/> <!-- Border color -->
        </shape>
    </item>

    <!-- Inner layer (actual circle) -->
    <item
        android:left="4dp"
        android:right="4dp"
        android:top="4dp"
        android:bottom="4dp">
        <shape
            android:shape="oval">
            <solid android:color="@android:color/white"/>
        </shape>
    </item>

</layer-list>
